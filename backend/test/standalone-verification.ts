import { INestApplication } from '@nestjs/common';

// Standalone verification script is executed via `node test/run-verification.mjs`
export const verificationNotice = 'Use node test/run-verification.mjs to execute specification verification.';
